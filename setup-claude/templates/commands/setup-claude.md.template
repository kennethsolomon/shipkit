---
description: "Bootstrap/repair .claude/ + tasks/ scaffolding for this repo."
---

<!-- Generated by /setup-claude -->

# /setup-claude

Bootstrap or repair Claude Code scaffolding for this repository.

## Steps

1. Run a dry-run preview:

   ```bash
   python3 "$HOME/.agents/skills/setup-claude/scripts/apply_setup_claude.py" "$(pwd)" --dry-run
   ```

2. If the preview looks correct, apply changes:

   ```bash
   python3 "$HOME/.agents/skills/setup-claude/scripts/apply_setup_claude.py" "$(pwd)"
   ```

3. If you previously ran `/setup-claude` and want to refresh generated files (marker-guarded):

   ```bash
   python3 "$HOME/.agents/skills/setup-claude/scripts/apply_setup_claude.py" "$(pwd)" --update-generated
   ```

4. Optional: print detection details (JSON):

   ```bash
   python3 "$HOME/.agents/skills/setup-claude/scripts/apply_setup_claude.py" "$(pwd)" --print-detection
   ```

## Output

After running, report:
- ✅ Created
- 🔄 Updated
- ⏭️ Skipped (and why)
